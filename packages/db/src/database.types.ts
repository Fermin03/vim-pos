// AUTOGENERADO por `supabase gen types typescript --local`.
// NO editar a mano. Se regenera tras cada migración (pnpm db:types).
// Placeholder hasta que exista el proyecto Supabase y se apliquen las migraciones.
export type Database = Record<string, never>;
