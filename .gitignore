# deps
node_modules/
.pnpm-store/

# builds
.next/
dist/
.turbo/
out/
*.tsbuildinfo

# env / secrets  (NUNCA commitear service_role ni JWT secret)
.env
.env.*
!.env.example

# supabase local
supabase/.branches/
supabase/.temp/

# os / editor
.DS_Store
*.log
.vscode/
.idea/

# claude code — config local de preview/tooling
.claude/

# Vercel
.vercel
supabase/functions/.env

# Carpeta que arma _capturas/empaquetar.mjs para subir a Hostinger.
# Es un artefacto derivado de sitio-web/, no fuente.
_publicar/
